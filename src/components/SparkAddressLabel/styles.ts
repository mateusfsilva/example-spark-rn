import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    address: {
        marginTop: 16,
        fontSize: 16,
        color: "#007AFF",
        textAlign: "center",
        fontWeight: "bold",
    },
    hint: {
        fontSize: 12,
        color: "#888",
        textAlign: "center",
        marginTop: 4,
    },
})
