import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        alignItems: "center",
        marginTop: 16,
    },
    invoice: {
        fontSize: 16,
        color: "#007AFF",
        textAlign: "center",
        fontWeight: "bold",
        paddingHorizontal: 16,
    },
    hint: {
        fontSize: 12,
        color: "#888",
        textAlign: "center",
        marginTop: 4,
    },
})
